<!-- Input: Python 源码 -->
<!-- Output: 可安装的 Python 包 -->
<!-- Pos: 源码根目录 -->

# `src/` 目录

> 一旦我所属的文件夹有所变化，请更新我。

架构（≤3行）：
- 遵循 `src-layout` 布局，包含核心业务逻辑包 `tx_news`。
- 隔离源代码与项目根目录配置，避免导入歧义。

## 文件
| 文件 | 地位 | 功能 |
| --- | --- | --- |
| `tx_news/` | 核心包 | 包含所有核心业务逻辑（Crawler, ETL, Storage, Agent）。 |
| `tx_news.egg-info/` | 元数据 | pip install -e . 生成的包信息（gitignored）。 |

## 工具调用上限修改说明

### 已完成修改
1. **工具调用上限**：
   - 将默认工具调用上限从 6 改为 50
   - 所有相关文件统一使用 50 作为最大值

2. **执行逻辑修改**：
   - 当达到工具调用上限时，不再直接中断
   - 强制 LLM 根据现有信息生成最终输出
   - 添加了友好提示，告知用户已达到最大工具调用次数
   - 确保在任何情况下都能返回有意义的结果

3. **文件修改位置**：
   - `src/tx_news/agent/txnews_agent.py`：修改了 `run` 和 `run_stream` 方法
   - `apps/api/main.py`：更新了 `ChatRequest` 模型的默认值
   - `README.md`：更新了文档中的默认值说明

## 前端动画实现建议

### 状态动画设计
1. **分析中状态**：
   - 添加旋转动画，如旋转的加载图标
   - 显示当前工具调用次数
   - 使用蓝色或中性色调

2. **分析停止状态**：
   - 添加渐变动画，如进度条完成效果
   - 显示已完成的工具调用次数
   - 使用绿色或成功色调

3. **分析失败状态**：
   - 添加错误提示动画，如闪烁或震动效果
   - 显示具体错误信息
   - 使用红色或错误色调

### 实现建议
1. **前端框架**：
   - 使用 CSS 动画或 CSS 变量控制动画状态
   - 结合 JavaScript 监听后端事件
   - 确保动画流畅，不影响用户体验

2. **事件监听**：
   - 监听 SSE 事件类型（delta, tool_call, tool_result, done）
   - 根据事件类型切换不同的动画状态
   - 处理超时和错误情况

3. **用户体验**：
   - 添加清晰的状态文字说明
   - 提供取消或重新开始的选项
   - 确保动画在不同设备上都能正常显示

## 后续优化建议

1. **前端代码实现**：
   - 在前端代码中添加状态管理
   - 实现不同状态的动画效果
   - 确保与后端事件格式兼容

2. **性能优化**：
   - 优化工具调用逻辑，减少不必要的调用
   - 添加缓存机制，避免重复查询
   - 优化 LLM 调用，减少延迟

3. **监控与日志**：
   - 添加工具调用次数统计
   - 记录工具调用耗时
   - 提供性能监控仪表盘
